MONGODB_URI=
POSTGRES_DATABASE_URL=
JWT_SECRET=
PORT=
# Optional – enables live Gemini LLM Insight in the productivity agent.
# Without it the agent returns a deterministic fallback insight.
GEMINI_API_KEY=


